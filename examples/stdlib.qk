print("clock: " + clock());
print("random: " + random());
let m: map < number > = {
  a: 1, b: 2
};
print("len(m) = " + len(m));
print("keys(m) = " + keys(m));
let s: string = "hello";
print("len(s) = " + len(s));
